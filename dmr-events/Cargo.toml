[package]
name = "dmr-events"
version = "2.4.0"
edition.workspace = true
license.workspace = true
description = "Bridge-side DMR call metadata, callsign lookup, and stats event types."

[dependencies]
dmr-types.workspace = true
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json.workspace = true

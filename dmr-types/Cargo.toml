[package]
name = "dmr-types"
version = "2.3.0"
edition.workspace = true
license.workspace = true
description = "Shared DMR identity and voice-frame types for asl-dmr-bridge."

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true

[dev-dependencies]
toml.workspace = true

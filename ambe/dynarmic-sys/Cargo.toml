[package]
name = "dynarmic-sys"
version = "1.4.0"
edition.workspace = true
license.workspace = true
description = "FFI bindings for the MD380 firmware vocoder via the dynarmic ARM JIT."
links = "md380_vocoder"

[build-dependencies]
cc.workspace = true
cmake = "0.1"
